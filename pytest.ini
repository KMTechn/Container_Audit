[pytest]
pythonpath = .
testpaths = tests
addopts = -ra -p no:deepeval
markers =
    real_gui: owned native Tk widgets; explicit capability skip when a display is unavailable
    native_e_drive: tests an unchanged native E-drive policy; skips when that capability is unavailable
